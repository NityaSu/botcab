# Railway — https://docs.railway.com/guides/dockerfiles
# Create a project, add Postgres + Redis plugins, then deploy this service from the repo.
# Map env vars from the plugins to SPRING_* (see DEPLOY.md).

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Liveness only — app process is up. Avoids flaky Redis/DB probes killing the deploy.
healthcheckPath = "/actuator/health/liveness"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
